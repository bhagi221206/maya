#include <stdio.h>

int main() {
    int N;
    
    // Read the number of ?2000 notes
    scanf("%d", &N);
    
    // Calculate the number of ?500 notes needed
    int result = N * 4;
    
    // Output the result
    printf("%d
", result);
    
    return 0;
}
